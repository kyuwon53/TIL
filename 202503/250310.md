# 📆 2025-03-10 (월)

## 🔍️ 오늘 무엇을 했나요? Review
### [WORK] 뷰어 전용 API 헤더 설계
X-Viewer-mode 헤더 도입 의도 정리: 뷰어/비뷰어 여부를 구분해 쓰는 API 필요. 뷰어 전용 API는 어노테이션이 없으면 만들어서 붙이는 방식으로 처리하기로 하고, 헤더명은 X-Viewer-only로 결정.

### [WORK] 토큰 발급 구조 개선
토큰 발급 방식을 단계적으로 전환하는 구조 정리:
- 기존: userIdx
- 중간 연결 단계: userIdx + memberId 동시 보유
- 최종: memberId만 사용

userId를 레거시에서 한빛앤 memberId로 전환하는 HBN-1179 작업의 전환 단계(컬럼 추가 → 데이터 싱크 → memberId 조회 저장 → userId 컬럼/로직 제거)와 일치하는 진행 단계.

🎯 내일 할 일
- [ ] user_contentmark 작업
