# 📆 2024-09-26 (목)

## 🔍️ 오늘 무엇을 했나요? Review
### [WORK] 배너 호출 API 설계
특정 위치의 배너만 호출되도록 typeId 기반 조회 방식을 검토했다. 코드 값을 URL에 그대로 노출하는 것이 적절한지 고민했다.
이후 배너 API는 pCode/product_title을 제거하고 headline을 추가하며, 랜딩용 pCode는 landingUrl로 전달하는 방식으로 개편되었다(HBN-614).

### [WORK] 카테고리 호출 API
카테고리 조회 API 작업을 진행했다.

### [WORK] 스프링 배치 환경 세팅 및 계획 수립
스프링 배치 환경을 세팅하고 배치 계획을 작성했다. batch 모듈을 구성하고 Spring Batch + 코틀린을 사용하며, entity는 data 모듈 것을 재사용하고 쿼리 작성은 batch 모듈 쪽에 두는 방향으로 정했다. 한빛(habit.co.kr) 기존 DB는 읽기 전용으로만 사용하고, 데이터 저장/수정은 운영툴에서 PostgreSQL로 처리한 뒤 저장된 PostgreSQL 데이터를 MySQL DB로 적재하는 흐름으로 설계했다.
데이터 마이그레이션 배치는 1) 배치 기본 세팅, 2) 기존 DB 데이터 마이그레이션 배치, 3) 강의 관련 데이터 이관 배치 작성의 3단계로 계획했다.
배치 모듈 기본 세팅은 젠킨스 빌드 분리와 application.yml 구성 작업(HBN-415)과 같은 맥락이며, 한빛 DB(MySQL)-한빛앤 DB(PostgreSQL) 간 양방향 배치 이관은 HBN-426/HBN-427 작업과 동일한 구조다.

### [WORK] kotest/mockk 학습
kotest DescribeSpec과 mockk를 사용한 목킹 테스트 작성법을 학습했다.
```kotlin
import io.mockk.mockk
import io.mockk.impl.annotations.MockK
import io.mockk.unmockkAll
import org.springframework.test.util.ReflectionTestUtils

class YourTest : DescribeSpec({

    @MockK
    lateinit var product: Product

    beforeEach {
        product = mockk()
        ReflectionTestUtils.setField(product, "id", 1L)
    }

    afterEach {
        unmockkAll()
    }

    describe("Your test description") {
        it("should set the field correctly") {
            // Your test assertions here
        }
    }
})
```

## 🎯 내일 할 일
- [ ] 이기종 DB 연결
- [ ] 간단 배치 생성 테스트
- [ ] 스터디
- [ ] 히스토리 테이블 생성
