# 📆 2025-04-21 (월)

## ☑️ 오늘 한 일 📑 ( 목표하지 않았지만 추가된 것들 )
- [x] 배치 트랜잭션 격리 수준 변경(병렬 가동 검증)
- [x] 이메일 인코딩 문제 해결
- [ ] 수정 로직 패키지 이동
- [x] 본인 인증 여부 업뎃 API 작업

## 🔍️ 오늘 무엇을 했나요? Review
### [WORK] 배치 트랜잭션 격리 수준 변경(병렬 가동 검증)
배치를 병렬로 가동시켜보기 위해 격리 수준을 read_committed로 변경, 개발 환경에 반영. 반영 전 마지막 배치 에러 발생 시각은 10:35, 개발 반영은 10:58에 완료.

### [WORK] 이메일 인코딩 문제 해결
application.yml의 기본 인코딩은 UTF-8이지만, EC2 서버 자체 로케일이 kr_UTF_8로 지정되어 있어 인코딩이 어긋나는 문제 확인. 값을 큰따옴표(`""`)로 감싸 지정하는 방식으로 해결. 운영 환경에도 동일하게 적용될지는 미확인 상태이며, 운영은 NCP 관련 파라미터를 깃허브 액션을 통해 주입하고 있음을 확인.

### [WORK] 본인 인증 여부 업데이트 API
본인 인증 여부를 업데이트하는 API 작업을 진행(세부 구현 내용은 기록되지 않음).

### [WORK] 배포 스크립트(Blue-Green) 점검
블루그린 배포 스크립트 로직을 점검. 신규 배포 전 기존 API 컨테이너를 정리하고 이후 신규 컬러 컨테이너를 기동하며, 각 단계 실패 시 로그를 남기고 반환하는 구조. 내부 Nginx 설정 경로, 환경별 배포 커맨드 예시, 환경별 설정 파일(config-location) 지정 방식을 확인.

### [WORK] 암호화 payload 비교 테스트
어떤 요청은 실패하고 어떤 요청은 성공하는지 비교하기 위해 암호화된 `info` 값과 테스트용 `key` 값으로 구성된 요청 payload를 기록. 실패 케이스만 기록되었고 성공 케이스 비교 내용은 문서화되지 않은 채로 작업이 중단됨(휴대폰 본인확인 서버-투-서버 전환 관련 작업으로 추정, OPS-419).
