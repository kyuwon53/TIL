# 📆 2024-10-02 (수)

## 🔍️ 오늘 무엇을 했나요? Review
### [WORK] Postgres ↔ MySQL 데이터 매핑 작업
이번 주 목표인 "Postgres → MySQL 데이터 마이그레이션 배치 작업 완료"의 일환으로, hb_class, hb_class_list, hb_class_mng 테이블 1차 매핑 작업을 완료했다.
- 매핑 과정에서 발견한 이슈는 Confluence 문서에 정리해 업데이트했다.
- 이슈 관련 내용 중 author 필드는 제외하고 매핑을 진행했다.
- 첨부 파일은 우선 빈 문자열로 처리해두었다.
  - TODO: 첨부 파일 zip으로 넣기, 과거 데이터의 여러 첨부파일도 zip으로 묶어 넣기
- update는 아직 고려하지 않고 insert 위주로 작업했다.
  - TODO: updatedAt과 createdAt을 비교해 update/insert를 분리하는 로직 필요(더 나은 방법이 있으면 대체)

### [WORK] 멀티 모듈 연동 이슈
앱 구동 시 멀티 모듈의 data 모듈 연결이 석연치 않게 얽혀 앱이 뜨지 않는 문제가 발생했다. 없는 필드나 엔티티를 참조하는 현상이 있었고, 해결이 오래 걸리면 data 모듈 연결을 해제하고 batch 쪽에 별도 entity-repo를 생성하는 방향도 검토하기로 했다.
- 오후 계획: 앱 실행 문제 해결 → 작업한 매핑분을 로컬 DB에 연동해 테스트 → 로직 정리

### [WORK] MySQL ↔ Postgres 배치(반대 방향 포함)
- MySQL → Postgres 방향 작업은 완료했다. 날짜 조건문 추가, 메소드 정리, 배치화(현재는 API 형태로만 존재)가 남았고, 개발 DB 업데이트는 완료했다. (관련: HBN-426 — start/endDate 파라미터로 HbClass를 읽어 한빛앤 DB 형식으로 가공·적재하는 배치 작업과 같은 방향)
- Postgres → MySQL 방향은 아직 테스트가 필요한 상태다. (관련: HBN-427 — 신규 스키마를 레거시 hb_class 컬럼으로 매핑하는 배치)

## 💡 오늘 회고
불투명함…그러나 할 수 있다아아악!!! 나… 할 수 있나..?

## 🎯 내일 할 일
- [ ] 첨부 파일 zip 처리(당일/과거 데이터 모두)
- [ ] updatedAt/createdAt 비교로 update·insert 로직 분리
- [ ] 앱 구동 시 멀티 모듈 연동 이슈 해결
- [ ] 매핑 작업분 로컬 DB 연동 테스트 및 로직 정리
- [ ] Postgres → MySQL 배치 테스트
